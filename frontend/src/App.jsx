import React from "react";
import { Routes, Route } from 'react-router-dom';

const App = () => {
  return (
    <Routes>
      <Router path='' element={} />
      <Router path='' element={} />
      <Router path='' element={} />
      <Router path='' element={} />
      <Router path='' element={} />
    </Routes>
  )
}

export default App